1. **Fix GitHub Actions Workflow `ci-cd.yml`**
   - The CI is failing for three main reasons:
     1. Deprecated `actions/upload-artifact@v3` (causing a hard failure).
     2. Deprecated Node 20 actions (`actions/checkout@v3`, `actions/setup-node@v3`, `actions/github-script@v6`).
     3. Missing `gas-report.txt` file when trying to read it in `actions/github-script`.
   - The Sentinel memory directs me:
     > PR Scope Directive: When executing bounded tasks (e.g., micro-UX improvements under 50 lines), do not bundle unrequested CI/CD version upgrades to resolve deprecation warnings. However, if a deprecated action (e.g., `actions/upload-artifact@v3`) causes a hard CI failure (not just a warning), you must upgrade it to the required version (e.g., `v4`) as the absolute minimum fix required to unblock CI.
     > CI Configuration: When using `actions/github-script` to read a file generated by a previous pipeline step (e.g., `fs.readFileSync('gas-report.txt')`), ensure the file is explicitly outputted or piped (e.g., `npm test | tee gas-report.txt`) during the test run.
   - I will use `replace_with_git_merge_diff` to:
     - Upgrade `actions/upload-artifact@v3` to `v4`.
     - Update the run command for the gas report to `REPORT_GAS=true npm test | tee gas-report.txt`.

2. **Verify Changes**
   - Use `run_in_bash_session` to execute `sed -n '75,80p' .github/workflows/ci-cd.yml` to verify `actions/upload-artifact@v4`.
   - Use `run_in_bash_session` to execute `sed -n '95,100p' .github/workflows/ci-cd.yml` to verify `REPORT_GAS=true npm test | tee gas-report.txt`.

3. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.**
   - Call the `pre_commit_instructions` tool to perform required pre-commit steps.

4. **Submit the PR**
   - Use `run_in_bash_session` to execute `gh pr create` with the branch name `sentinel/fix-weak-randomness`.
   - The PR description will describe the CI fix.
